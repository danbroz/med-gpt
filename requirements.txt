requests>=2.31
numpy>=1.26
pyarrow>=15.0
pytest>=8.0

# Local-host requirements for the FAISS-training orchestrator:
#   * `ssh` and `rsync` binaries on PATH (system packages, not pip).
# The worker that runs *on the B200 pod* installs `faiss-gpu-cu12`,
# `pyarrow`, `numpy` itself.
